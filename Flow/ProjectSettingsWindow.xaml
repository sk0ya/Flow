<Window x:Class="Flow.ProjectSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:vm="clr-namespace:Flow.ViewModels"
        Title="プロジェクト設定"
        Width="760" Height="720"
        MinWidth="620" MinHeight="560"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="{DynamicResource AppWindowBackgroundBrush}"
        FontFamily="Segoe UI">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="48" ResizeBorderThickness="6" CornerRadius="8" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBackground" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBackground" Property="Background" Value="{DynamicResource AppAccentSubtleBrush}"/><Setter TargetName="ButtonBackground" Property="BorderBrush" Value="{DynamicResource AppAccentOutlineBrush}"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBackground" Property="Background" Value="{DynamicResource AppAccentSubtleStrongBrush}"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogPrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="{DynamicResource AppAccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AppAccentTextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppAccentStrongBrush}"/>
        </Style>

        <Style x:Key="DialogInput" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="InputBackground" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True"><Setter TargetName="InputBackground" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/><Setter TargetName="InputBackground" Property="BorderThickness" Value="2"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogComboItem" TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBackground" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="6"><ContentPresenter/></Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True"><Setter TargetName="ItemBackground" Property="Background" Value="{DynamicResource AppAccentSubtleBrush}"/></Trigger>
                            <Trigger Property="IsSelected" Value="True"><Setter TargetName="ItemBackground" Property="Background" Value="{DynamicResource AppAccentSubtleStrongBrush}"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogCombo" TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DialogComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="ComboBackground" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="7"/>
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="▼" FontSize="8" Margin="8,0,0,0" VerticalAlignment="Center" Foreground="{DynamicResource AppTextMutedBrush}"/>
                            </Grid>
                            <ToggleButton x:Name="PART_ToggleButton" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Focusable="False">
                                <ToggleButton.Template><ControlTemplate TargetType="ToggleButton"><Rectangle Fill="Transparent"/></ControlTemplate></ToggleButton.Template>
                            </ToggleButton>
                            <Popup x:Name="PART_Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False">
                                <Border Margin="0,4,0,0" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" Background="{DynamicResource AppSurfaceBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="1" CornerRadius="8">
                                    <ScrollViewer MaxHeight="220"><ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/></ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger SourceName="PART_ToggleButton" Property="IsMouseOver" Value="True"><Setter TargetName="ComboBackground" Property="BorderBrush" Value="{DynamicResource AppAccentOutlineBrush}"/></Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="ComboBackground" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/><Setter TargetName="ComboBackground" Property="BorderThickness" Value="2"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,12,0,5"/>
        </Style>

        <Style x:Key="SettingsTabItem" TargetType="TabItem">
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{DynamicResource AppTextSecondaryBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AppSurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderSoftBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="TabBackground"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7,7,3,3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter ContentSource="Header"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TabBackground" Property="Background" Value="{DynamicResource AppAccentSubtleBrush}"/>
                                <Setter TargetName="TabBackground" Property="BorderBrush" Value="{DynamicResource AppAccentOutlineBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="TabBackground" Property="Background" Value="{DynamicResource AppAccentSubtleStrongBrush}"/>
                                <Setter TargetName="TabBackground" Property="BorderBrush" Value="{DynamicResource AppAccentBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource AppAccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition Height="*"/><RowDefinition Height="54"/></Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource AppSurfaceBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="18,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="プロジェクト設定" FontSize="15" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                    <TextBlock Text="  名前・用途・ボード構成" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}" VerticalAlignment="Center" Margin="8,2,0,0"/>
                </StackPanel>
                <Button HorizontalAlignment="Right" Style="{StaticResource DialogButton}" Padding="8,4" Margin="0" Click="OnCloseClick" ToolTip="閉じる">
                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="10"/>
                </Button>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="18,14,18,10" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource AppTextPrimaryBrush}">
            <TabItem Header="基本情報" Style="{StaticResource SettingsTabItem}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="2,14,2,20">
                        <TextBlock Text="プロジェクトの名前と用途を設定します" FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}"/>
                        <TextBlock Text="ここで変更するのはプロジェクト全体の情報です。タスクの内容はタイムラインから編集します。" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}" TextWrapping="Wrap" Margin="0,4,0,16"/>
                        <Border Padding="14" Background="{DynamicResource AppSurfaceAltBrush}" BorderBrush="{DynamicResource AppBorderSoftBrush}" BorderThickness="1" CornerRadius="9">
                            <StackPanel>
                                <TextBlock Text="基本情報" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                                <TextBlock Text="プロジェクトを識別するための情報" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}" Margin="0,2,0,5"/>
                                <TextBlock Text="プロジェクト名" Style="{StaticResource DialogLabel}" Margin="0,10,0,5"/>
                                <TextBox Style="{StaticResource DialogInput}" Text="{Binding ProjectName, UpdateSourceTrigger=PropertyChanged}"/>
                                <TextBlock Text="用途" Style="{StaticResource DialogLabel}"/>
                                <ComboBox ItemsSource="{Binding ProjectKindOptions}" SelectedItem="{Binding SelectedProjectKindOption, Mode=TwoWay}" Style="{StaticResource DialogCombo}">
                                    <ComboBox.ItemTemplate><DataTemplate DataType="{x:Type vm:ProjectKindOption}"><TextBlock Text="{Binding Name}" Foreground="{DynamicResource AppTextPrimaryBrush}"/></DataTemplate></ComboBox.ItemTemplate>
                                </ComboBox>
                                <TextBlock Text="{Binding SelectedProjectKindOption.Description}" FontSize="10" Foreground="{DynamicResource AppTextSecondaryBrush}" TextWrapping="Wrap" Margin="0,6,0,0"/>
                            </StackPanel>
                        </Border>
                        <Border Margin="0,14,0,0" Padding="14" Background="{DynamicResource AppInfoSurfaceBrush}" BorderBrush="{DynamicResource AppInfoBrush}" BorderThickness="1" CornerRadius="9">
                            <StackPanel>
                                <TextBlock Text="用途を変えても、タスクは消えません" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource AppInfoBrush}"/>
                                <TextBlock Text="レーンや分類を初期状態に戻したい場合は、「ボード構成」タブのテンプレート操作を使います。" FontSize="10" Foreground="{DynamicResource AppTextSecondaryBrush}" TextWrapping="Wrap" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="ボード構成" Style="{StaticResource SettingsTabItem}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="2,14,2,20">
                        <TextBlock Text="タスクを置く場所と分類を管理します" FontSize="12" Foreground="{DynamicResource AppTextSecondaryBrush}"/>
                        <TextBlock Text="レーンは作業の置き場所、分類はタスクの種類です。" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}" Margin="0,4,0,14"/>
                        <Border Padding="14" Background="{DynamicResource AppSurfaceAltBrush}" BorderBrush="{DynamicResource AppBorderSoftBrush}" BorderThickness="1" CornerRadius="9">
                            <StackPanel>
                                <TextBlock Text="テンプレート" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}"/>
                                <TextBlock Text="用途に合ったレーンと分類をまとめて追加します。既存タスクのデータは保持されます。" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}" TextWrapping="Wrap" Margin="0,2,0,5"/>
                                <TextBlock Text="{Binding ProjectPresetImpactSummary}" FontSize="10" Foreground="{DynamicResource AppTextSecondaryBrush}" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                <Button Content="選択した用途の構成を追加" Style="{StaticResource DialogPrimaryButton}" Margin="0,10,0,0" Command="{Binding ApplyProjectPresetCommand}"/>
                            </StackPanel>
                        </Border>

                        <Grid Margin="0,14,0,0">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="14"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Padding="14" Background="{DynamicResource AppSurfaceAltBrush}" BorderBrush="{DynamicResource AppBorderSoftBrush}" BorderThickness="1" CornerRadius="9">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="レーン" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}"/><TextBlock Text="作業の置き場所" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}"/></StackPanel>
                                        <Button Grid.Column="1" Content="＋" Style="{StaticResource DialogButton}" Padding="8,4" Margin="0" Command="{Binding AddLaneCommand}" ToolTip="レーンを追加"/>
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Lanes}" Margin="0,9,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:LaneViewModel}">
                                                <Grid Margin="0,3,0,0">
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                    <TextBox Style="{StaticResource DialogInput}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="1" Content="削除" Style="{StaticResource DialogButton}" Padding="6,4" Margin="5,0,0,0" Foreground="{DynamicResource AppDangerBrush}" Command="{Binding DataContext.DeleteLaneCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                            <Border Grid.Column="2" Padding="14" Background="{DynamicResource AppSurfaceAltBrush}" BorderBrush="{DynamicResource AppBorderSoftBrush}" BorderThickness="1" CornerRadius="9">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <StackPanel><TextBlock Text="分類" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AppTextPrimaryBrush}"/><TextBlock Text="タスクの種類" FontSize="10" Foreground="{DynamicResource AppTextMutedBrush}"/></StackPanel>
                                        <Button Grid.Column="1" Content="＋" Style="{StaticResource DialogButton}" Padding="8,4" Margin="0" Command="{Binding AddCategoryCommand}" ToolTip="分類を追加"/>
                                    </Grid>
                                    <Grid Margin="0,9,0,3">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <TextBox Style="{StaticResource DialogInput}" Text="{Binding NewCategoryName, UpdateSourceTrigger=PropertyChanged}" ToolTip="分類名を入力"/>
                                        <Button Grid.Column="1" Content="追加" Style="{StaticResource DialogButton}" Padding="7,5" Margin="5,0,0,0" Command="{Binding AddCategoryCommand}"/>
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Categories}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate DataType="{x:Type vm:CategoryViewModel}">
                                                <Grid Margin="0,3,0,0">
                                                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                                    <Ellipse Width="12" Height="12" Fill="{Binding Brush}" VerticalAlignment="Center" Margin="0,0,7,0"/>
                                                    <TextBox Grid.Column="1" Style="{StaticResource DialogInput}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                                    <Button Grid.Column="2" Content="削除" Style="{StaticResource DialogButton}" Padding="6,4" Margin="5,0,0,0" Foreground="{DynamicResource AppDangerBrush}" Command="{Binding DataContext.DeleteCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" Background="{DynamicResource AppSurfaceBrush}" BorderBrush="{DynamicResource AppBorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="18,0">
                <Button Content="閉じる" Style="{StaticResource DialogPrimaryButton}" Padding="16,7" Margin="0" Click="OnCloseClick"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
